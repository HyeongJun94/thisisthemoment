// media type
const ANDROID = 1;
const PC = 0;

// media size
const PCMinWidth = 1280;
const TabletMinWidth = 720;
const AndroidMinWidth = 360;

export default {
  ANDROID,
  PC,
  PCMinWidth,
  TabletMinWidth,
  AndroidMinWidth,
};
